# 接入契约：需前端实现或替换为实际 accessibilityIdentifier。禁止文字、XPath、坐标兜底。
schema_version: 1
strategy: accessibility_id
contract_status: requires_app_instrumentation
ids:
  navigation.messages: message.navigation.messages
  navigation.account: message.navigation.account
  navigation.back: message.navigation.back
  account.ready: message.account.ready
  account.identity: message.account.identity
  account.logout: message.account.logout
  account.logout_confirm: message.account.logout_confirm
  account.login: message.account.login
  login.username: message.login.username
  login.password: message.login.password
  login.submit: message.login.submit
  home.ready: message.home.ready
  home.system: message.home.system
  home.interactions: message.home.interactions
  home.hidden: message.home.hidden
  home.create_group: message.home.create_group
  home.system_unread: message.home.system_unread
  home.interaction_unread: message.home.interaction_unread
  conversation.menu: message.conversation.menu
  conversation.hide: message.conversation.hide
  chat.ready: message.chat.ready
  chat.identity: message.chat.identity
  chat.details: message.chat.details
  chat.composer: message.chat.composer
  chat.input: message.chat.input
  chat.send: message.chat.send
  group.ready: message.group.ready
  group.owner_controls: message.group.owner_controls
  group.pin: message.group.pin
  group.description: message.group.description
  group.visibility: message.group.visibility
  group.applications: message.group.applications
  group.dissolve: message.group.dissolve
  create.ready: message.create.ready
  create.name: message.create.name
  create.description: message.create.description
  create.visibility: message.create.visibility
  create.agreement: message.create.agreement
  create.submit: message.create.submit
  target.ready: message.target.ready
  notification_list.ready: message.notification_list.ready
  group.first: message.group.first
  conversation.row: message.conversation.{id}
  chat.message: message.chat.message.{id}
  notification.row: message.notification.{id}
  notification.read: message.notification.{id}.read
  interaction.category: message.interactions.category.{category}
  application.row: message.application.{id}
  application.decision: message.application.{id}.{decision}
  home_conversations.ready: message.home_conversations.ready
  home_conversations.total: message.home_conversations.total
  home_conversations.item: message.home_conversations.item
  home_conversations.end: message.home_conversations.end
  home_conversations.container: message.home_conversations.container
  hidden_conversations.ready: message.hidden_conversations.ready
  hidden_conversations.total: message.hidden_conversations.total
  hidden_conversations.item: message.hidden_conversations.item
  hidden_conversations.end: message.hidden_conversations.end
  hidden_conversations.container: message.hidden_conversations.container
  interactions.ready: message.interactions.ready
  interactions.total: message.interactions.total
  interactions.item: message.interactions.item
  interactions.end: message.interactions.end
  interactions.container: message.interactions.container
  owner_controls.ready: message.owner_controls.ready
  owner_controls.total: message.owner_controls.total
  owner_controls.item: message.owner_controls.item
  owner_controls.end: message.owner_controls.end
  owner_controls.container: message.owner_controls.container
  private_conversations.ready: message.private_conversations.ready
  private_conversations.total: message.private_conversations.total
  private_conversations.item: message.private_conversations.item
  private_conversations.end: message.private_conversations.end
  private_conversations.container: message.private_conversations.container
  conversation.preview: message.conversation.{id}.preview
